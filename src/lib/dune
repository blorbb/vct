(library
 (name vct)
 (libraries minisat)
 (inline_tests)
 (preprocess
  (pps ppx_inline_test)))

(menhir
 (modules parser))

(ocamllex lexer)

; disable unused open warning

(env
 (dev
  (flags
   (:standard -w -33-39))))
